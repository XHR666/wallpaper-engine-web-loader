# .gitignore —— 本仓库（we-scene-demo = WE-Scene 浏览器端渲染器）的忽略清单
#
# ⚠ 口径来源：本文件是 `.gitignore.public` 的**超集**。
#   `.gitignore.public` 仍然在位，因为 `publish-check.mjs` 把它当作**唯一的忽略清单**读取
#   （脚本里 `IGNORE_FILE = .gitignore.public`）——改它等于改发布闸门的行为，故保持原样不动。
#   **两份文件必须同步**：`.gitignore.public` 是"发布面"的规范来源（发布闸门认它），
#   本文件在它之上补 git 侧的额外排除（历史备份、归档、临时产物、编辑器垃圾）。
#
# 设计原则（与 `.gitignore.public` 一致）：**仓库只带"能跑起来的渲染器 + 自带样例 + 文档 + 测试"**，
# 本机数据（上报、私有库清单、基线、实验残留、真实壁纸语料）一律不进仓库。

# ── 本机运行数据 ──
reports/
*.log
/tmp-*
tmp/
*.tmp

# ── 私有库清单 / 基线（内嵌本机壁纸与工坊路径，**绝不能发布**）──
library-manifest.json
package-matrix.json
package-baseline.json
perf-matrix.json
perf-baseline.json

# ── 实验/调试残留 ──
*.bak
*.bak-*
*.bak*
*-probe.mjs
hina-probe.mjs
refrender-*.json
ref-render-*.json
ref-*.json
*AUDIT-preview-before.png
*AUDIT-preview-after.png
*.orig
*.rej

# ── 归档目录：`archive/` 是历史脚本与旧许可备份，**不属于发布面** ──
# （`publish-check.mjs` 的 SKIP_DIRS 也已跳过它，两边口径一致）
archive/

# ── 依赖与缓存 ──
node_modules/
__pycache__/
*.pyc
.DS_Store
.vscode/
.idea/
*.swp

# ── 本机 WE 安装（只读引用，绝不入库）──
wallpaper_engine/

# ── 说明（沿用 `.gitignore.public` 的口径，勿删）──
# 1) 测试壁纸（allwallpaper/**）留在**仓库外**；本仓库**不分发任何真实壁纸**。
#    这里**故意不写忽略规则** —— 让误加回来的大包继续被 `publish-check.mjs` 的体积闸门
#    （单文件 >100MB 阻塞）当场拦下，而不是被静默忽略掉。
# 2) `common*.h` 不是忽略项而是**待替换项**：忽略掉它们会让渲染器编译失败，故**故意不忽略**。
# 3) `vendor-ref/**`（webwallgl 等**仓库外**研读副本）位于工作区根，不在本目录内，无需规则；
#    其内容一律不入库（`publish-check.mjs` 也确认 "未 vendored"）。
# ── WE 官方资产（**运行时**从用户自己的 WE 安装读取，我们不分发）──
# 本项目内部 shader 只有 6 个头被实际引用（common.h / common_blending / common_blur /
# common_composite / common_fragment / common_perspective），这 6 个已换成自研实现；
# `common_vertex.h` **零引用** → 直接不随仓库分发（用户 WE 目录里有真品）。
common_vertex.h

# ── GitHub Pages 构建产物（build-pages.mjs 的输出，绝不入库）──
_site/
