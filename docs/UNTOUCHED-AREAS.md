# 未敢动的区域与已知未知（用户要的"说清楚一点"）

> 目的：把"我**没有**改、也不知道对不对"的地方一次列清，便于用户用自己的真机观察辅助判断。
> 判断规则：**有官方/上游证据支撑的才改**；拿不到证据的列在这里，不猜着改。
> 每条的"能定案的东西"= 需要什么证据才能动手（多数只需要用户在真机上看一眼某个现象）。

| # | 区域 | 为什么没动 | 现有证据 | 能定案的东西 | 动它的风险 |
|---|---|---|---|---|---|
| A | **多 additive 动画层合成顺序 + 蒙皮矩阵**（`sampleCompositeAdditivePose` / `bindRT` / 帧0 基准） | 这是用户报"眉毛翻转/眼睛乱动"最可能的所在，但改一处就影响**所有**蒙皮角色（6 个包里 5 个有 puppet） | 皮肤数据本身干净：32 骨全帧无 >90° 跳变、尾部 14 帧是三条动画逐位相同的导出垃圾；真机 10s 探针无镜像（`mirrorEver:[]`）、最大位移 2.82px。**而上游 webwallgl 渲染同一个包眼睛正常** ⇒ 差异在"我们怎么用它" | **上游渲染器同函数的逐步对照**（正在做）；或用户用上游截一帧眼睛正常的画面 + 我们同一时刻的截图 | 高（一旦方向错，全部角色一起坏） |
| B | **`blendIndices` / `blendWeights` 权重表是否正确** | 没有任何 ground truth；权重分错骨会产生**正好**是"眉毛被搬走/翻过去"的形态 | 只能证明"网格能画出来"，不能证明"每根骨权重对" | 上游 `mdl.js` 的权重读法对照（同一份 MDL，两边的顶点→骨映射应当逐字节一致） | 中 |
| C | **`MDLE0002` 骨骼扩展矩阵（`bones[b].extend`）** | 段解析了，但语义不明（注释写"逆向自 wallpaper64.exe，IK/约束相关"），**从未参与计算** | 段存在且每骨 64B；不接它时多数包看着正常 | 官方/上游是否使用该段（上游 `mdl.js` 有没有读 MDLE）；若上游也不用 ⇒ 可以确认"不需要" | 低（不用它=现状） |
| D | **层/子网格可见性**（puppet 内部子网格粒度） | 我们的 `?ln` 只到**层**粒度，看不到 puppet 内部的子网格；用户要"单层隔离看眉毛"做不到 | hina 37 个对象里没有眼睛/眉毛层（脸是一张 `materials/人物.tex`） | 给渲染器加"子网格隔离"探针（按 `blendIndices` 分组只画某些骨影响的顶点）——**这是我能自己做的取证工具**，不需要用户 | 低（只加探针） |
| E | **逐属性脚本**（`origin/color/... = {script:...}`） | 我们不跑脚本宿主，只读作者保存的静态快照 | 6 个相机包里 5 个的 `origin` 是脚本；施加快照会整幅偏 −2434px ⇒ 默认**不施加**（已量化） | 需要"逐属性脚本宿主"才能真跑；官方语义缺证据 | 中（影响取景/配色） |
| F | **后处理 / 全屏效果链（HDR/bloom）** | 只做了能证实的部分；用户报"上游开了后处理会改颜色，关掉后背景/粒子静止、音频条变白矩形" | 上游把**逐帧推进**挂在后处理链上（结构性提示，正在核实代码位置） | 上游后处理链的结构图（正在做）；我们的 `?nofx=1` 与上游开关的逐帧对照 | 中 |
| G | **抗锯齿 / DPR / 粒子强度**（上游新版有 UI 可调） | 我们是靠 `?dpr`/`?psize` 之类散开关，没有统一面板 | 上游 README 1.3.16 有 AA 与媒体修复条目 | 上游的参数通路（正在做） | 低 |
| H | **鼠标跟随语义**（不开指针注入也能跟随；移出画面后原地转圈） | 我们默认 `lockToPointer` 需指针信息，没指针就不发射 | 用户实测上游：不注入也跟随，但**移出画面后在离开点转圈**（bug） | 上游坐标来源（正在做）；我们要同时给"移出即停" | 低 |
| I | **Web 类壁纸（iframe + WE API shim）** | 完全没实现（我们只做 scene；视频在插件侧） | 上游 README 明确 web 壁纸走 sandbox iframe + API shim | 要不要做，取决于用户需求优先级 | 高（新子系统） |
| J | **透视相机 `fov`**（①**P-107 已实现，本项结案为"有样本 + 有实现"**） | ~~`buildCamera` 只有正交分支；语料 15 个相机对象全部带 `orthogonalprojection` ⇒ 无样本~~ 现已有样本：`allwallpaper/0917/3509243656`（语料 38 个容器里**唯一** `general.orthogonalprojection = null` 的包） | ①(P-107 2026-09-17) `buildCamera` 新增透视档（`mat4Perspective`）+ `?projmode=persp\|ortho\|auto`（默认 `auto`）：该包读数为 142 对象 / 8 模型 / 4 粒子 / 59 图 / 53 文本、对象 z ∈ [−50, +6]（96 个 z=0、24 个 z=5.1）、相机层 `origin` 静态 `0 0 6` + `zoom 1` + `fov = {user:"newproperty71"}`（project.json 里 text="视场"、slider ∈[40,65] step 0.1）⇒ auto 走**节点锚定**透视（fov 50 / 相机 z=6）。量化：`Custom BG`(z=−50) 与 `Sun png`(z=−30) 屏宽比 3.1072 = 透视律预测值、近(z=5.1)/远(z=−50) 同世界偏移屏距比 62.2×、fov 40→65 屏宽 ×1.519；正交路径**逐位不变**（3 真包 × 3 档 + 合成包 × 5 档冻结对拍全 `===`） | **还缺的证据**：① 相机 `angles`（朝向）未接 —— 该包相机没有 angles，有 angles 的 3D 包出现再补；② 3D 网格/蒙皮的透视接线（`MESH_VS` 仍按设计画布 1:1 映射，粒子 CPU NDC 仍走自己的 `perspCam` 通路）；③ 官方侧只有"行为对照"（第三方参考实现的 `global_perspective` 用相机 z=1000 + `fov=atan(h/1000/2)×2`），无 WE 官方文档口径 | 低（新档只在"无正交矩形 **且** 声明了 fov"的包上由 auto 生效；`?projmode=ortho` 逐位回退，`?projmode=persp` 对正交包的 z=0 平面只差 Float32 舍入 1.7e-4px） |
| K | **文本层 `limitwidth/limitrows` 换行语义** | 只按经验实现 | 上游 1.3.16 修过"宽度受限换行被 2x2 占位盒挤压"⇒ 有对照价值 | 上游文本画布逻辑对照 | 低 |
| L | **音频响应效果（audio processing combos）** | 只接了部分；系统采集后端在本机不存在 | 上游 `live-system.ts` 有完整媒体源（含"模拟源"回落） | 上游音频通路对照 | 中 |
| M | **凯尔希 `?eyehack`** | 是**经验补丁**（长条眼窗），不是原理性修复 | 量化过：眼缝 −307.68 → −47.11px（官方 −23.27） | 上游对"眼睛 mesh 摆放/范围"的处理方式 | 中 |

> **状态追加（2026-09-17，P-109 / 任务书 P1-4）——只追加，上面各行历史结论一字未改**：
> **D 项：工具已交付 ⇒ 结案为"有探针 + 有实测分组"**。新增 `?submesh=<骨筛选>` / `?subtri=all|major|any`
> （`core/we-scene-bundle.js`，**默认关、只读、默认路径逐位不变**：测试用"反向变异回旧写法"对拍 GL 调用序列），
> 按 `blendIndices` 主影响骨把 hina `人物` 网格分成 **29 组 / 497 顶点**，并出台账（每组 bbox/质心/顶点数/
> 影响骨表/父链/三角形归属数/**蒙皮后位移时程**/**三角形有向面积变号**计数），可与 `?bones=` 叠加；
> 实测面部 = **眼 b16(22 顶点)+瞳孔 b30(18)**、**眉毛 b17(11)**、**睫毛/上眼睑 b18(15)+外眼角 b19(12)**
> （骨 16/30/17/18/19，父链 17←15←4←3←0 与 16←15←4←3←0），UV 裁剪与空间排布双向印证
> ⇒ "眉毛必然是骨/蒙皮权重的产物"这条**已在骨+顶点粒度落实**（见 PATCHES P-109.1）。
>
> **A 项：新增一条候选根因（数值，未动手）**。用探针台账追到骨一级后，**bind 世界链的乘法顺序**
> （现渲染器 `matMulRow(bindWorld[parent], bind[b])` = 父先乘）与动画采样链
> （`sampleAnimRT` 的"子先乘/行向量正确序"）**不是同一空间**：前者与"动画帧 0（该帧逐骨局部量 == `bind` 局部量）"
> 相差最大 **340.76 px**、与"骨应落在自己顶点附近"平均差 **122.1 px**；换成子先乘后 32 骨 maxΔ = **0.0000 px**、
> 平均差 **47.3 px**。静止帧看不出来（`gBones = bindInv × bindRT = I`，逐位正确），一旦动画走动，
> `final = bindRT + Σ(p_k(t) − p_k(0))` 把**错序空间的基准**与**正确序空间的增量**相加 ⇒ hina 实测
> **眉毛 b17 在 t=0.33s 整组 8/8 三角形变号**、**睫毛 b18 位移 102.07px @8.50s / 14-16 条变号 @0.40s**
> （周期恰为 8.0s = 动画 2 的 240 帧）；只换成一致链序，同为 **b17 0/8、b18 11.1px**（29 组平均最大位移 14.48→8.07px）。
> ⇒ A 项"能定案的东西"从"等上游/等用户截图"变成"**先定链序语义**"（仍属 A 项，须按 A 的口径做全语料回归；
> 本轮**未改渲染器**，只出证据）。官方侧：静态 `W1.jpg` 面部正常；`preview.gif` 只有 192×192（眼宽 ≈1.7px）⇒
> 像素级判读不可能，改用它做"事件窗口内暗像素重心跨度 0.65px vs 对照 0.46px"的粗对照，**没看到**我们那 5.1 GIF px
> 量级的位移（支持"这是我们的口径产物"，但不是官方逐帧语义的真值）。B/C 两项**未动**（权重表仍无上游 ground truth）。
>
> **状态追加（2026-09-17，P-110 / 任务书第 1 项）——只追加，上面各行历史结论一字未改**：
> **A 项：已定位并修复（引 P-110）**。根因 = **bind 世界链的乘法顺序与动画链相反**：P-109.3 定的候选根因成立，
> 修法是把 bind 链改成与 `sampleAnimRT` 同空间的**子先乘** `W[b] = L_b × W[parent]`（唯一实现处
> `core/puppet-skin.js::bindWorldChain`，4 处调用点共用；旧写法只由 `?bindorder=legacy` 启用），
> 并同时定下 `gBones` 的组装顺序 = **`bindInv × m`**（判据：静止帧恒等式**不能**判序（两种顺序都给 `I`），
> 能判序的是"绕骨骼枢轴的刚性旋转" —— `bindInv × m` 保距 2.6e-5px、`m × bindInv` 把顶点甩到离枢轴 490.29px；
> elysia 移植侧的 `m × bindInv` 因此判为上游同类缺陷，只读不改）。
> 实测（`tests/bind-order-test.mjs`，5 个蒙皮包 + 1 个无 puppet 对照）：**眉毛 b17 整组翻转 8/8 → 0/8**、
> **睫毛 b18 位移 102.07px → 11.09px**、29 组变号三角 25 → 1；静止帧逐顶点对拍 maxΔ=1.6e-5px（不改静止画面）、
> 6 包网格 bbox 与层矩形**逐位不变**；残余变号（b16 1/17）经三条机器判据定性为**多骨权重剪切（LBS 固有）**
> —— 单骨三角形永不翻（65582 个样本 0 变号）、在"全旧/半修/全修"三种口径下条数完全相同（与链序无关）。
> B/C 两项仍未动（权重表无上游 ground truth）；`elysia` 侧的同类缺陷与"6 个残余三角形是否肉眼可见"仍列未定
> （P-110.6）。⇒ 本项的"能定案的东西"从"等上游对照"落实为"**链序语义 + 全语料回归**"，**待用户在真机看一眼
> 眉毛那 0.3–0.9s 是否还有异动**即可关闭最后一条（像素级对照，本机无 GPU 拿不到）。

## 需要用户配合的最小取证清单（每条只需看一眼/截一张）
1. **A**：用上游 webwallgl 打开 `3554161528`，在**眼睛正常**时截图，并在同一场景时间点截我们的画面（同一 URL 加 `?ln=11`）。→ 可直接定位是合成顺序还是权重。
2. **F**：上游"后处理开关"打开/关闭各截一张，+ 关掉后音频条变白矩形那张。→ 判断"逐帧推进挂在后处理上"是否成立。
3. **H**：鼠标移出画面后原地转圈的那一瞬间（能录一小段最好）。→ 确认是"没有 mouseleave"还是"坐标被 clamp"。
4. **A**：若能在官方 WE 里看一次同包的眨眼/呼吸（哪怕手机拍屏），给"官方到底动不动眉毛"一个真值。

## 我自己就能推进的（不需要用户）
- **D**：加"子网格隔离探针"（按骨分组只画部分顶点），把"眉毛到底是哪几根骨、哪几个顶点"钉死。
- **B/C**：与上游 `renderer/vendor/we-scene/render/mdl.js` 做逐字节对照（同一份 MDL，比较骨骼/权重/动画三个表的读法）。
- **G/K**：照上游参数通路补齐 UI。
