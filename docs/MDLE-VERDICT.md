# MDLE-VERDICT —— `MDLE0002`（骨骼扩展矩阵）中立判决书

> **这是什么**：针对"**我们的语料里有没有包真的依赖 `MDLE0002` / 要不要额外支持它**"这个问题，
> 把**事实**（语料实测、代码路径、第三方行为结论）与**结论**分开写清的**中立判决书**。
> 它是审计项 **N-5**（"仓里没有判决文档"）的交付物，取代此前只在 `docs/UNTOUCHED-AREAS.md`
> 与 `docs/PATCHES.md` 里零散出现的判断。
> **性质**：**证据页**，不是愿望页 —— 支持与反对两方的证据都在 §3 并列；结论只在 §4 出现，并附**翻案判据**。
> **日期**：2026-09-18 · **方法**：只读扫描本机语料 + 只读代码走查 + 引用第三方**行为结论**（不取代码）。
> **许可纪律**：`wer-ref` 是 **GPL-2.0-only** ⇒ **只引用其行为结论（测试断言字符串）与行号，不取任何代码**；
> `vendor-ref`（`oneincase/webwallgl`，**MIT**）按参考区纪律**只引用行为结论与实测数字，不复制其代码/注释/文案**。
> **用词**：本页描述我方实现一律写「**独立实现**」。

---

## 1. 语料实测（命令 + 计数）

### 1.1 语料规模与容器版本

```bash
$ cd "$MPW_ROOT/allwallpaper" && find . -type f \( -name '*.pkg' -o -name '*.mpkg' \) | wc -l
98
$ find . -type f \( -name '*.pkg' -o -name '*.mpkg' \) | sed 's/.*\.//' | sort | uniq -c
     77 mpkg
     21 pkg
```

98 个容器的**容器头**分布（只读扫描，0.5 s，无错误）：

```
PKGV0024=6  PKGV0023=7  PKGV0022=6  PKGV0021=1  PKGV0020=1  PKGM0018=10  PKGM0014=67
```

> 即：语料**不是一个版本**。`scene.pkg` 系的 `PKGV00xx` 共 21 个（0020–0024 横跨 5 个版本），
> `*.mpkg` 系 77 个（`PKGM0014` / `PKGM0018`）。

### 1.2 `scene.json` 与它引用的模型

- **53 个容器**含 `scene.json`（`.mpkg` 里多数没有）。
- `scene.json` 的 `objects[].image` 指向 **`models/*.json`** 描述符：语料共 **387 条** `models/*.json` 条目。
- 这些描述符的**首个键**（"头"）分布：

| 首键 | 数量 | 类型（按 JSON 内容） |
|---|---|---|
| `autosize` | 332 | 其中 **297** 为 sprite/material（`material` 字段），**35** 为 **puppet**（`puppet` 字段 → `.mdl`） |
| `height` | 37 | sprite/material |
| `instanced` | 18 | sprite/material |

> 读法：**绝大多数模型不是骨骼模型**（350/387 为贴图/精灵描述符）；**35 条** puppet 描述符才指向 `.mdl`。

### 1.3 `.mdl` 的**模型头**（本页关键计数）

语料内共 **43 条** `.mdl` 条目，其**前 8 字节**：

| 模型头 | 条目数 |
|---|---|
| `MDLV0023` | **26** |
| `MDLV0019` | 14 |
| `MDLV0016` | 1 |
| `MDLV0021` | 2 |

> ⚠ **修正一处旧口径**：`docs/PATCHES.md` 里"语料 puppet 模型头是 `MDLV0023`"的说法
> 只对**多数**成立 —— 语料里另有 **14 条 `MDLV0019`**、2 条 `MDLV0021`、1 条 `MDLV0016`。
> **`MDLV0019` 的模型不参与本页 MDLE 结论**（见 1.4：带 MDLE 的 6 个文件全部是 `MDLV0023`）。

### 1.4 `MDLE0002` 到底出现在哪里

**43 条 `.mdl` 条目里 8 条带 `MDLE0002`**（= **6 个不同文件**；`夜莺night` 的 `.mpkg` 在本机有两份副本、
`Rella 部件_puppet.mdl` 同时出现在 `3448877775` 与夜莺 `.mpkg` 里 ⇒ 8 条条目由 6 个文件构成）：

| 容器 | 模型 | 模型头 | `MDLE0002` |
|---|---|---|---|
| `0917/3448877775/scene.pkg` | `models/Rella 部件_puppet.mdl` | `MDLV0023` | 有 |
| `0917/3462491575/scene.pkg` | `models/眼睛_puppet.mdl` | `MDLV0023` | 有 |
| `0917/3462491575/scene.pkg` | `models/m眼睛_puppet.mdl` | `MDLV0023` | 有 |
| `dd/3544152633/scene.pkg` | `models/girl_puppet.mdl` | `MDLV0023` | 有 |
| `dd/3554161528/scene.pkg` | `models/人物_puppet.mdl` | `MDLV0023` | 有 |
| `dd/3719111841/scene.pkg` | `models/眼睛组合_puppet.mdl` | `MDLV0023` | 有 |
| `wallpaperE/other/夜莺night——…rella].mpkg` | `models/Rella 部件_puppet.mdl` | `MDLV0023` | 有（与上行同文件） |
| `wallpapertest1/夜莺night——…rella].mpkg` | `models/Rella 部件_puppet.mdl` | `MDLV0023` | 有（同上，第 3 份副本） |

**同一文件的核对**（sha256 实跑）：

```bash
$ node <探针>  # 读出 pkg 条目并 sha256
models/Rella 部件_puppet.mdl @ 3448877775: size=94707 sha256=cdf34bb617ffd5c81350a6144ae792d4c35c08c051650971a07d5d7b7389055a
models/Rella 部件_puppet.mdl @ 夜莺night:      size=94707 sha256=cdf34bb617ffd5c81350a6144ae792d4c35c08c051650971a07d5d7b7389055a
```

### 1.5 段内数据长什么样（逐骨实测）

按 `elysia` 解析器同款布局（`[MDLE0002\0][u32 段尾偏移][u32 骨骼矩阵字节][每骨 64B]`）逐骨读出 127 个 64 B 矩阵：

| 指标 | 结果 |
|---|---|
| 骨数一致性（`MDLE` 矩阵数 vs `MDLS` 骨数） | **8/8 一致**（127 骨全部对齐） |
| **全零** payload 的模型数 | **0/8**（矩阵全部非零） |
| 与同下标 `MDLS` **局部** bind **逐字节相同**的骨 | **68 / 127** |
| 与 `MDLS` 局部 bind **不同**的骨 | **59 / 127** |
| 差异骨是否被网格蒙皮引用（`blendIndices` + 权重 > 0） | **是**：Rella 10/10、眼睛 1/1、m眼睛 1/1、girl 1/1、人物 18/20（2 个未被引用）、眼睛组合 5/6（1 个未被引用） |

**关键中立探针**：把 MDLE 校正 `corr[b] = World(MDLE)[b] · World(MDLS bind)[b]⁻¹` 作用到**静止顶点**上，
量每个顶点相对原网格的位移（**两种合成序都算**，避免约定之争）：

```
0917/3448877775  Rella 部件   v=144 父先乘 {maxDisp:3.93, maxBone:6, verts>0.5:12/144} | 子先乘 同值
0917/3462491575  眼睛         v=278 父先乘 {maxDisp:0.00} | 子先乘 {maxDisp:0.00}
0917/3462491575  m眼睛        v=42  父先乘 {maxDisp:0.00} | 子先乘 {maxDisp:0.00}
dd/3544152633    girl         v=438 父先乘 {maxDisp:0.00} | 子先乘 {maxDisp:0.00}
dd/3554161528    人物         v=497 父先乘 {maxDisp:0.00} | 子先乘 {maxDisp:0.00}
dd/3719111841    眼睛组合     v=372 父先乘 {maxDisp:0.00} | 子先乘 {maxDisp:0.00}
（夜莺 mpkg 两份副本 = Rella 部件同文件，3.93 同上）
```

> **一句话**：带 `MDLE0002` 的 6 个模型里，**6 个**用 MDLS bind 与用 MDLE 作基准的**静止结果一致**
> （唯一非零是 Rella 部件的 **3.93** 单位 / 144 顶点中的 12 个）；**两种合成序结论相同**。

---

## 2. 代码怎么处理（`file:line`）

| 位置 | 事实 |
|---|---|
| `elysia/we-renderer/puppet.js:481-482` | 注释：`MDLE0002`（骨骼扩展矩阵，每骨 64 B，IK/约束相关，逆向自 `wallpaper64.exe`）+ 布局描述 |
| `elysia/we-renderer/puppet.js:483-500` | **唯一解析处**：`buf.indexOf('MDLE')` → 读段尾偏移/矩阵字节 → 逐骨读 64 B |
| `elysia/we-renderer/puppet.js:496` | `bones.forEach((b, i) => { if (mats[i]) b.extend = mats[i] })` —— 写入 `bones[].extend` |
| **全仓 `.extend` 的读取方** | **0**：`grep -rIn "\.extend\b" elysia core` 只有上面这一处**写**；`core/we-scene-bundle.js` 里 `extend` 出现 **0** 次 |
| `core/we-scene-bundle.js` | `MDLE` **0** 命中（运行期 bundle 完全不解析该段） |
| `demo.html` | `MDLE` **0** 命中 |
| `core/puppet-skin.js:28` | 只校验模型头 `MDLV`；`:52` 只定位 `MDLS`（bind）——**不找 MDLE** |
| `core/attach-transform.mjs:152,185-186` | 只处理 `MDLS` + `MDLA` |
| `packages/**`、`tests/**`、`shaders/`、`server/`、`web/` | `MDLE` **全 0 命中**（没有门禁断言、没有测试覆盖该段） |

**结论（代码层）**：现状是"**运行期完全不解析；参考实现侧解析但不消费**"——
`elysia` 的 `b.extend` 是一个**只写不读**的字段，不存在"半实现导致误用"的代码路径。

---

## 3. 支持与反对"需要额外支持 `MDLE0002`"的证据

### 3.1 支持方（认为**需要**额外支持）的证据

1. **段真实存在且非平凡**：6 个不同 `.mdl`（`MDLV0023`）带该段；**0/8 全零**；骨数 8/8 与 `MDLS` 对齐（§1.5）。
2. **差异骨不是"死角落"**：59/127 个与 `MDLS` 局部 bind **逐字节不同**的骨里，**绝大多数被网格蒙皮引用**
   （Rella 10/10、眼睛 1/1、m眼睛 1/1、girl 1/1、人物 18/20、眼睛组合 5/6）⇒ 若上游把 MDLE 当绑定基准，理论上会改变姿势。
3. **第三方给出过"它是绑定姿势"的描述**：`references/vendor-ref`（`oneincase/webwallgl`，**MIT**，仅行为对照、非构建依赖）
   在解析层把该段描述为"**贴图空间的静止姿势**"，并给出实测：顶点按 `MDLS` 烘焙、而 UV 对应 MDLE 姿势；
   对案例 `lainpw` 用 `World(MDLE)·World(MDLS)⁻¹` 变换后，与 UV 反推的贴图坐标**误差为 0**。
   同一文件另一处注释把它称作"蒙皮的绑定基准（缺失时用 `MDLS`）"。
4. **我们自己的实现保留了解析**：说明当时认为该段"值得解析"（`elysia/we-renderer/puppet.js:481-500`）。

### 3.2 反对方（认为**不需要**额外支持）的证据

1. **一手中立实测（本页最强证据）**：把上述校正作用到**我们语料**的静止顶点上，位移 **≈ 0** ——
   6 个模型里 5 个 **< 0.01** 单位、无一顶点位移 > 0.5；唯一非零是 Rella 部件骨 6 的 **3.93** 单位（12/144 顶点），
   **两种合成序结论相同**（§1.5）⇒ 在我们语料上，"用 MDLS bind"与"用 MDLE"**给出同一结果**，"额外支持"没有可观测收益。
2. **同一份 vendor-ref 的蒙皮路径记录了实测反证**（`references/vendor-ref/.../render/mdl-skin.js:276-292`）：
   乘该校正会把案例 `lainpw`（模型 `3787341007`）的眼睛推离 **426 px / 360 px**、头发 **345 px**，
   而**不乘时顶点偏离原网格 0 px**；文中据此**明确要求不要乘**该校正，并佐证其本机唯一一直正常的 puppet
   （`3791428510`）**没有** MDLE 块。⇒ 该实现自己的行为结论是：**MDLE 不参与蒙皮**。
3. **`wer-ref`（GPL-2.0-only，只引用行为结论）**：`src/backend/scene/internal/parser/WPMdlParser.cpp:835,842` 校验
   "payload 字节数必须 = 骨数 × 64，否则**拒绝整个模型**"；`:1026` 分派到该段并登记为 `file_world_bind`
   （`has_file_world_bind = true`）。但它的测试**逐字**断言：
   `src/test/scene/mdl/puppet_semantics_test.cpp:101` → "**MDLE must not replace the validated MDLS root bind**"；
   `:185` → "**unset MDLE payload must remain observational only**"。⇒ 行为结论 = **记录/校验，但不用于绑定姿势**。
4. **`lwe-ref` 全树 0 命中**（根本不存在对该段的处理）。
5. **我们运行期 0 命中 + `b.extend` 只写不读**（§2）⇒ 现状不存在需要修的"半实现"。
6. 现状渲染**没有**已知的 MDLE 相关视觉缺陷报告（多个含 MDLE 的包在现行管线里正常出图）。

### 3.3 两方证据的对照

| 争点 | 支持方 | 反对方 | 本页采信 |
|---|---|---|---|
| 段是否存在、是否非平凡 | 6 文件、非零、骨数对齐 | — | **支持方**（事实） |
| 差异骨是否可能影响姿势 | 被蒙皮引用 | 校正位移 ≈ 0（我们语料） | **反对方**（一手实测优先） |
| 第三方是否"用它" | 曾被称为绑定基准 / 贴图空间静止姿势 | 同实现的蒙皮路径实测**禁止**用它；另两个实现不用于计算 | **反对方**（2:1，且反证是同源的实测） |
| 我们是否需要补功能 | 解析但未消费=潜在缺口 | 消费与否在我们语料上结果相同 | **反对方** |

---

## 4. 结论与判据

### 4.1 结论

**维持现状：不需要为 `MDLE0002` 增加额外支持（该段不参与计算）。**

三条理由，按证据强度排序：

1. **一手语料实测**：在该段存在时，"`MDLS` bind 基准"与"`MDLE` 基准"在我们语料上**静止结果等价**
   （校正位移 ≤ 3.93 单位 / 仅 1 个模型 12 个顶点；其余 < 0.01），两种合成序结论一致。
2. **三方行为结论一致**：`lwe-ref` 不解析；`wer-ref` 解析+校验但测试断言不得替换 `MDLS` root bind、未设 payload 仅作观察；
   `vendor-ref` 解析但在蒙皮路径**实测禁止**使用（乘它会把部件推出数百 px）。
3. **我方运行期零依赖**：bundle / `demo.html` 0 命中，`elysia` 的 `b.extend` 只写不读，无测试断言 ⇒ 不存在误用风险。

> **这不是"作废 / 根本不存在这个问题"**：段真实存在、数据非平凡、且被蒙皮引用。
> 结论只在"**我们的语料 + 我们的渲染路径上不需要额外支持**"这一层成立 —— 换语料或换上游证据都要重开（见 4.2）。

### 4.2 翻案判据（满足任一条即重新打开本判决）

| # | 触发条件（可操作判据） | 动作 |
|---|---|---|
| **A** | **真机/官方对照出现系统性错位**：对含 MDLE 的 6 个模型（优先 `dd/3554161528`、`dd/3544152633`、`0917/3448877775`、夜莺 `.mpkg`）做真机截图对照，发现贴图/部件位置**系统性**偏移，且**以 MDLE 为基准后偏移消失** | 升级为实现任务；先写复现脚本 + 前后截图 hash |
| **B** | **上游行为证据**：拿到"上游/官方在蒙皮或贴图对齐里**实际使用**该段"的可复核证据（行为实验、日志、或非 GPL 来源的实现） | 重跑 §1.5 探针 + 加真机 A/B，再改结论 |
| **C** | **语料变化**：新语料里出现校正位移大于**视觉阈值**（建议：> 模型包围盒对角线的 1%）的包，或出现新形态（全零 / 骨数不匹配 / 非 64 B 步长） | 重跑 §1.5 全部探针；按新数据重判 |
| **D** | **结案条件（反向）**：对 A 里的 6 个模型做完真机对照，确认贴图/姿态**无**系统性错位 | 可结案为"我们的语料不需要额外支持"，并把 `elysia` 的解析降为注释级观察项（**不删**，见 `docs/REMOVALS.md` §3） |

### 4.3 对其它文档的影响（只给指针，不代改）

- `docs/UNTOUCHED-AREAS.md` 的 C 行（`MDLE0002`）可据此把"语义不明 / 上游是否使用"标注为**已判**（判决=不需要，附翻案判据）。
- `docs/PATCHES.md` 的"`MDLE0002`（C）仍未参与任何计算"仍是**事实陈述**，与本判决不冲突。
- `docs/REMOVALS.md` §3 已登记"`elysia` 的 MDLE 解析**不删**"（观察项 + 翻案基础）。

---

## 5. 未证实项（本页**没有**证明的东西）

1. **59/127 个差异矩阵的语义未证实**。一个**未证实的假设**（不作为结论）：它们是同一世界位姿的另一种局部参数化
   —— 依据是校正 `World(MDLE)·World(bind)⁻¹ ≈ I`，但本页没有字节级/上游证据。
2. **Rella 部件骨 6 的 3.93 单位位移**是数值分解残差还是真实校正，未证实。
3. **vendor-ref 的实测模型不在本机语料**：`3787341007`、`3791428510` 在本机 `allwallpaper` 下 `find` **命中 0** ⇒
   其"426 px / 345 px"等数字**不能**外推到我们的语料（只能作为方法学与反证方向的证据）。
4. **我们从未做过"开 / 关 MDLE"的真机 A/B 截图对照** —— 4.2-D 的结案条件因此尚未满足。
5. **命名口径冲突未解**：`wer-ref` 把该段登记为 `file_world_bind`（"世界绑定"），而本页探针显示其值多与**局部** bind
   逐字节相同、且与两种世界链合成序都**不**匹配。双方对 "world" 的定义差异未证实（可能一方是"文件里的世界绑定"、
   另一方是"运行时合成的世界矩阵"）。
6. **上游（官方引擎 / 官方 Web 版）是否使用该段**：本页**没有**官方来源证据，只有两个第三方实现的行为结论。
7. **`MDLE` 段头里的 "u32 段尾偏移" 字段语义未证实**：探针观察到它指向条目末尾附近（与段大小自洽），但本页不据此下任何结论。
8. **`MDLV0019` 的 14 个模型未被检查**是否也能带 MDLE（本页只在带 MDLE 的 8 条 `MDLV0023` 条目上做结论）。

---

## 附录：复跑方法（全部只读）

```bash
# ① 容器头 + scene.json 引用 + models/*.json 首键 + .mdl 模型头 + MDLE 命中（Node，约 0.5 s，只读）
node <容器扫描探针>        # 输出见 §1.1–1.4；本页所有计数来自该输出
# ② 逐骨对比：MDLE 64B 矩阵 vs MDLS bind 局部 / 世界链 / 蒙皮引用 / 校正位移
node <MDLE 探针>           # 输出见 §1.5
# 关键阈值（②里写死的判据，便于复核）
#   - 矩阵"相同" = 16 个 float32 逐字节相等
#   - 顶点"被引用" = blendIndices 槽位(40/44/48/52) 的骨号且对应权重(56/60/64/68) ∈ (0,1]
#   - 位移 = 对静止顶点施加 corr[b] 后的欧氏距离；报 maxDisp / 位移>0.5 的顶点数
# ③ 代码面 0 命中核对
cd "$MPW_ROOT/we-scene-demo"
grep -c MDLE core/we-scene-bundle.js demo.html        # → 0 0
grep -rIn "MDLE0002" . --exclude-dir=.git --exclude-dir=docs --exclude-dir=assets   # → 只有 elysia/we-renderer/puppet.js
grep -rIn "\.extend\b" elysia core                     # → 只有一处写
```

> 探针脚本本轮在 `/tmp` 下运行（**未随仓库分发**，因为它们是取证工具而非产品代码）；
> 复跑逻辑已在上面的阈值里写死，任何人可据此重建。
